export default{
    props:{
        post:{
            url: String,
            name: String,
            mChStart: Array,
            date: Number,
            like: Number,
            disLike: Number
        }
    }
}